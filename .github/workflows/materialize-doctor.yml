name: Materialize Hoplite doctor

on:
  push:
    branches:
      - agent/hoplite-doctor-108

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Add local runtime doctor
        run: python3 .github/scripts/materialize-doctor.py
      - name: Format and validate generated changes
        run: |
          rustfmt --edition 2021 core/src/doctor.rs core/src/diagnostics.rs
          python3 -m json.tool docs/public-surfaces.json >/dev/null
          python3 -m json.tool docs/core-boundary.json >/dev/null
          test ! -e .github/workflows/materialize-doctor.yml
          test ! -e .github/scripts/materialize-doctor.py
          test ! -e .github/scripts/doctor.rs.gz.b64
          git diff --check
      - name: Commit the implementation and remove this workflow
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && {
            echo "materialization produced no changes" >&2
            exit 1
          }
          git commit -m "Add local runtime doctor"
          git push origin "HEAD:${GITHUB_REF_NAME}"
