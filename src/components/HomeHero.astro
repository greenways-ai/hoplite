---
import ThemedArtwork from "@greenways-ai/visual-language/ThemedArtwork.astro";
const install = "git clone https://github.com/greenways-ai/hoplite.git && cd hoplite && make example-dev";
const base = `${import.meta.env.BASE_URL.replace(/\/+$/, "")}/`;
const artworkBase = "https://opensource.greenways.ai/visual-language/artwork/hoplite/";
---
<section class="hoplite-hero">
  <ThemedArtwork class="hoplite-hero__art" light={`${artworkBase}rabbit-courtyard-day.webp`} dark={`${artworkBase}rabbit-courtyard-night.webp`} lightMobile={`${artworkBase}rabbit-courtyard-day-mobile.webp`} darkMobile={`${artworkBase}rabbit-courtyard-night-mobile.webp`} lightAlt="An alert rabbit sitting naturally in a golden formal courtyard" darkAlt="The rabbit resting naturally in the moonlit courtyard" position="center" />
  <div class="hoplite-hero__veil"></div>
  <div class="hoplite-hero__copy">
    <p class="technical-kicker"><i></i> Hara packages · Git-native deployment</p>
    <h1>Small. Fast. Yours.</h1>
    <p>Configure signed Hara packages in <code>project.edn</code>, clone the project, and run it. Hoplite turns the same locked service into a local process or verified container—with authentication and management built in.</p>
    <div class="hero-links">
      <a class="action action--gold" href={`${base}getting-started/`}>Get started <span>→</span></a>
      <a class="action" href={`${base}reference/cli/`}>Read the reference</a>
      <a class="action" href="https://github.com/greenways-ai/hoplite">Source ↗</a>
    </div>
    <div class="hero-command"><code>{install}</code><span>clone · run · ship</span></div>
  </div>
</section>
