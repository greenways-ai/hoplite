[package]
name = "hoplite-value-store-sqlite"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
rust-version = "1.78"
description = "SQLite implementation of Hoplite's opaque canonical-value store"

[dependencies]
hoplite-value-store = { path = "../value-store" }
rusqlite = { version = "0.32", features = ["bundled"] }
sha2 = "0.10"

[workspace]
