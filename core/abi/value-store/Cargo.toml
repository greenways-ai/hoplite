[package]
name = "hoplite-value-store"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
rust-version = "1.78"
description = "Opaque canonical-value compare-and-swap contracts for Hoplite hosts"

[dependencies]

# Keep this first implementation independently testable without changing the
# production workspace lock. It can become a root workspace member when the
# provider adapter is wired into Hoplite.
[workspace]
