[package]
name = "hoplite-value-provider-filesystem-ffi"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
rust-version = "1.78"
description = "C ABI for the installed filesystem-backed hoplite.value provider"

[lib]
crate-type = ["staticlib"]

[dependencies]
hoplite-value-provider-filesystem = { path = "../value-provider-filesystem" }

[dev-dependencies]
sha2 = "0.10"

[lints.clippy]
# Test-only digest fixture; production provider paths remain warning-denied.
format_collect = "allow"

[workspace]
